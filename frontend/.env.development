## 开发环境（推荐）
## 说明：
## - 前端请求默认走 /api（见 src/utils/request.ts）
## - Vite 会把 /api 代理到 VITE_API_PROXY_TARGET

# 后端服务地址（Vite 代理目标），一般就是同一台电脑上的后端端口
VITE_API_PROXY_TARGET=http://127.0.0.1:8080

# 前端请求基地址（开发环境建议保持 /api 走代理）
VITE_API_BASE_URL=/api

# 如需生成可给手机扫的二维码/链接，把这里改成你电脑的局域网 IP，例如：http://192.168.1.13:5173
VITE_PUBLIC_WEB_ORIGIN=http://172.20.10.4:5173

